sudo: false
dist: xenial
language: python
cache:
  directories:
  - "$HOME/.cache/pip"
python:
  - "3.6"
  - "3.7"
install:
- pip install --upgrade pip wheel
- pip install -r requirements.txt
- make install
script:
- make test
